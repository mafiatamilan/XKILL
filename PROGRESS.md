# XKILL Backend — Build Progress

A module is only checked off once its unit tests AND its e2e suite are green and both have
been shown to the user. Update this file immediately after each module passes, before moving
to the next one.

## Modules

- [x] 5.1 Auth & Identity
- [x] 5.2 Student Platform
- [x] 5.3 College Academic Module
- [x] 5.4 Placement Preparation
- [x] 5.5 / 5.20 DSA Platform — 5.5a core solve loop (browse/filter problems, run/submit, Judge0 verdicts via BullMQ queue + Socket.io `submission.verdict` event, editorial, progressive hints, submission history) + 5.5b (playlists, sheets, progress, analytics, visibility, discussion) + 5.5c (contests, Elo ratings, live leaderboards, anti-cheat ingestion) — complete
- [x] 5.6 AI Interview Engine — multi-turn mock interviews (hr/technical/dsa/system-design), AI opening question + per-turn next question/feedback, state machine (created→in_progress→ended), fresh-AI transcript summarization with truncation, dsa-type turns graded by the real JudgeService against the catalog problem, technical-type context from SkillProfile, text-only mode with explicit 501 for voice/video — complete
- [x] 5.5b DSA Platform — playlists (private/shared, 404-no-existence-leak), curated sheets (Blind 75, Grind 169, NeetCode 150, Google, Amazon) seeded from a real problem catalog, live progress/analytics computed from Submission rows (accuracy, heatmap, weak/strong topics, runtime, rating-trend placeholder), recruiter visibility settings behind the pure `filterVisibleFields` predicate, per-problem discussion with idempotent upvotes
- [ ] 5.6 AI Interview Engine
- [x] 5.7 AI Career Coach — long-horizon career roadmap (phases over months-years, distinct from 5.4's tactical 10-week executor), learning recommendations, personalized salary prediction (explicitly `isEstimate: true`, confidence + range + factors, revisit vs 5.16 reference data later), deterministic skill-gap analysis vs the target role, and a request/response AI chat assistant with full history persistence + 5.6-style AI-failure resilience — complete
- [x] 5.8 Resume Builder & ATS — CRUD + seeded templates, snapshot-on-save version history (restore creates a NEW version, never overwrites), deterministic ATS score (`computeAtsScore`: section presence, ATS-hostile elements, text density, job-description keyword overlap) + AI suggestions with graceful degradation (503 `AI_SUGGESTIONS_FAILED`, score still persisted), lightweight `GET /score` + `GET /suggestions` re-reads, on-demand PDF (`@react-pdf/renderer`) + DOCX (`docx`) exports from current or historical content — complete
- [x] 5.9 Coding Battles — ranked/practice/private 1v1 matches with live WebSocket sync, deterministic matchmaking, Elo ratings, invite-code private battles — complete
- [x] 5.10 Leaderboards — read-only, Redis sorted-set backed leaderboards (global via `RankingService`, college/department/company/weekly/monthly/nearby-me via raw SQL), pagination, city-scoped nearby-me — complete
- [x] 5.11 Gamification — XP ledger (idempotent), levels, streaks (timezone-aware), badges/achievements, daily rewards, missions, weekly challenges, seasonal events — complete
- [x] 5.12 Certificates — issue, public verify (no auth), QR, PDF export, renew (1yr extend), LinkedIn share URL, certificate number sequencing, revocation support — complete
- [x] 5.13 Job Marketplace — CRUD (recruiter-owned), public search/filters, apply, save/unsave, eligibility check, contact recruiter, company profiles — complete
- [x] 5.14 Internship Portal — CRUD (recruiter-owned), public search/filter (summer/winter/remote/hybrid), apply, certificate issuance, company profiles — complete
- [x] 5.15 Mentor Marketplace — Mentor profiles (faculty/student-owned), availability management, booking with double-booking prevention ($transaction), payment, review, complete flow — complete
- [x] 5.16 Company Preparation Paths — Per-company hiring patterns, interview questions, OA info, salary insights, prep timeline, public prep-path endpoint — complete
- [ ] 5.16 Company Preparation Paths
- [ ] 5.17 Recruiter Portal
- [ ] 5.18 TPO Portal
- [ ] 5.19 Faculty Portal
- [ ] 5.21 Notification & Communication
- [ ] 5.22 Analytics & Reporting
- [ ] 5.23 Search & Discovery
- [ ] 5.24 Payments & Subscriptions
- [ ] 5.25 AI Services (shared)
- [ ] 5.26 Community
- [ ] 5.27 System Administration
- [ ] 5.28 College Programming Lab Module

## Cross-cutting deliverables

- [x] Complete Prisma schema, migrations committed, indexes on filter/sort columns
- [x] Swagger docs live at /api/docs, every endpoint documented
- [x] Global exception filter, validation pipe, logging interceptor wired at app level
- [x] Seed script populating realistic data across every module
- [x] docker-compose.yml running Postgres, Redis, Judge0, API together
- [x] Unit coverage ≥85% per module; full e2e suite green; golden-path smoke test green
- [x] GitHub Actions CI: lint → unit → e2e → coverage gate on every PR
- [x] README.md with setup instructions, architecture overview, link to API docs

## Test counts & coverage

| Module | Unit tests | E2E tests | Coverage (stmts/branch/funcs/lines) |
|---|---|---|---|
| 5.1 Auth & Identity | 187 | 64 | 95.46 / 90.1 / 85.48 / 95.16 |
| 5.2 Student Platform | 99 | 24 | — (module-level: students 100% funcs, repo 100%, readiness 100%) |
| 5.3 College Academic Module | 183 | 33 | — (module-level 96.83 / 81.93 / 98.23 / 96.66; faculty service 100%, repo 97.7 / 82.5 / 97.18 / 97.64, calculators 100% funcs) |
| 5.4 Placement Preparation (incl. shared AiService) | 61 | 13 | — (full-suite stmts/branch/funcs/lines 96.24 / 84.41 / 92.8 / 96.11) |
| 5.5a DSA Platform core (incl. JudgeService, BullMQ worker, Socket.io gateway) | 49 | 18 | — (dsa 95 / 72.09 / 88.7 / 94.55; judge 91.89 / 71.05 / 100 / 90.9) |
| 5.5b DSA Platform organize & track (playlists, sheets, progress, analytics, visibility, discussion) | 74 | 18 | — (calculators 98.46 / 90.54 / 90.9 / 98.9; track service + controller + repository fully unit-covered) |
| **Full suite (cumulative)** | **664** | **170** | **95.23 / 83.18 / 91.73 / 95.09** |

Coverage gates (global thresholds in package.json): statements ≥85, branches ≥80, functions ≥85, lines ≥85.

## Decisions log

Record anything you had to ask the user to clarify, plus their answer, so later modules stay
consistent with earlier ones.

| Date | Question | Answer |
|---|---|---|
| 2026-08-01 | Which auth flows should the first module (5.1) cover? | Full JWT access+refresh rotation, email verification, password reset, TOTP 2FA, and OAuth (Google/GitHub/LinkedIn) with RBAC role/permission admin — per the module spec. |
| 2026-08-01 | Local dev ports already in use? | Yes — local Postgres on 5432 and Redis on 6379 were taken. `.env` uses 5433/6380 for the XKILL containers; `.env.example` keeps 5432/6379 as defaults. |
| 2026-08-01 | 5.2 readiness-score/recalculate is a POST that mutates score state — should it count toward activity engagement in the score itself? | No — recalculation logs a `readiness` activity entry, but `countRecentActivity` excludes the `readiness` type so a recalc never inflates its own input (keeps consecutive recalcs deterministic). |
| 2026-08-01 | 5.2 `GET /readiness-score` before any recalc — return value? | Returns 200 with an empty body (Nest serializes `null` returns as empty 200). The dashboard exposes `readinessScore: null` explicitly. Client treats the empty 200 / absent `overall` as "no score yet". |
| 2026-08-02 | 5.3 Who may create/assign faculty to subjects — faculty or admin? | Both. Faculty `POST /faculty/subjects` self-assigns; admin `POST /admin/courses` can optionally pass `facultyId`. A subject is always owned by exactly one faculty member, and every faculty write path (materials/attendance/assignments/exams/marks/question-bank) re-checks ownership → 403 `SUBJECT_NOT_ASSIGNED` otherwise. |
| 2026-08-02 | 5.3 `enterBulkMarks` rollback semantics in e2e | Transactional: a mid-batch failure (invalid student, `marksObtained > maxMarks`) throws inside `$transaction`, so zero rows persist — e2e asserts the mark count is unchanged after a forced 2-row failure. |
| 2026-08-02 | 5.3 Student `@Resource('academics-*')` permissions | The student seed role now grants `read:academics-*` (plus `create:academics-assignments` for submission); without these the RolesGuard 403s every `/academics/*` request. Discovered via the e2e suite. |
| 2026-08-02 | 5.4 AI backend — which AI provider should the shared AiService call? | User override: NOT Claude/Anthropic. Use **opencode**. Initial wiring used `@opencode-ai/sdk` against a headless `opencode serve`, but the user then pinned the call to the opencode **Responses API** at `https://opencode.ai/zen/v1/responses` with a Bearer API key. |
| 2026-08-02 | 5.4 How should AiService reach `https://opencode.ai/zen/v1/responses`? | **Direct HTTPS fetch** (chosen by user) — drop `@opencode-ai/sdk` entirely. `createAiClient` keeps the SDK's session/prompt shape so `AiService` is unchanged, POSTs the OpenAI-compatible body `{model, instructions, input:[{role, content:[{type:'input_text',text}]}]}`, and reads `output[].content[].output_text`. |
| 2026-08-02 | 5.4 AI auth/credential model | `OPENCODE_API_KEY` (Bearer) — currently empty in `.env`; user will supply it when needed. Base URL default `https://opencode.ai/zen/v1/responses`. No basic-auth username/password anymore. |
| 2026-08-02 | 5.4 How do the placement readiness prediction and 5.2 ReadinessScore relate? | The prediction **extends** the 5.2 scorer — `calculateReadinessScore` (5.2) stays the single source of truth for the base score; placement adds roadmap progress + target-company count on top: composite = 70% readiness + 30% progress, ×0.9 penalty when no target companies, levels high≥75 / medium≥50 / low, monthsToReady 1/3/6. |
| 2026-08-02 | 5.4 How should the AI-dependent study planner be tested? | Mock the AI client everywhere (jest `moduleNameMapper`-free — plain mocked `fetch` in unit specs, `FakeAiService` overriding `AiService` in e2e). No real server in CI. `AiService.generateStructured` retries once on malformed/invalid responses, then throws a clean `AiServiceError`; upstream HTTP errors propagate immediately. |
| 2026-08-02 | 5.5a 5.28a was specified as "extend JudgeService" — but no JudgeService/BullMQ/Socket.io gateway existed in the repo and Judge0 wasn't in docker-compose. | 5.28a never actually landed. Built the shared judge layer from scratch inside 5.5a: `JudgeService` (single-run + batch multi-case grading with aggregated verdict & first-failing-case detail), BullMQ `dsa-submissions` queue + `WorkerHost` processor, and a Redis-adapter Socket.io gateway (`user:{id}` rooms) that 5.9/5.10/5.28 reuse. |
| 2026-08-02 | 5.5a Judge0 verdict mapping — self-hosted CE has no dedicated MLE status. | Mapped 3→accepted, 4→wrong_answer, 5→time_limit_exceeded, 6→compilation_error, 7–14→runtime_error, 15→memory_limit_exceeded. Non-Judge0 grading errors surface as submission status `failed` with `errorMessage` + a failed WS event. |
| 2026-08-02 | 5.5a Progressive hint unlock semantics | Hint 1 is always unlocked; a user may only unlock hint N after hint N−1 is already unlocked (`HINT_LOCKED` 404 otherwise). `getHints` returns `content: null` + `isUnlocked: false` for not-yet-unlocked hints. |
| 2026-08-02 | 5.5a Solved-state under concurrent submissions | `SolvedProblem` unique `[userId, problemId]` upserted only on Accepted — concurrent double-submission is winner-take-all and never double-counts (e2e asserts exactly 1 row). |
| 2026-08-02 | 5.5a Socket.io adapter client lifecycle | ioredis v5 auto-connects, so `connectToRedis` must await `ready` (not call `.connect()`), and `RedisIoAdapter.close()` must quit pub/sub after `super.close()` — otherwise the leaked clients hang the Jest e2e process ("did not exit"). |
| 2026-08-02 | 5.4 Roadmap lazy generation + persistence | `GET /placement/roadmap` generates a personalized 10-week × 7-task roadmap on first access (persisted), reused afterwards; week `tasks` are returned via the seed permission `update:placement-tasks` for completion. |
| 2026-08-02 | 5.5b Discussion upvote tracking — count field vs join rows | Both: a denormalized `upvoteCount` on `Discussion` plus a `DiscussionVote` join table (`@@unique([discussionId, userId])`) so upvotes are idempotent (one vote per user, never double-counted) and list queries still sort/filter by count cheaply. Upvote is POST-only (no downvote/un-vote yet). |
| 2026-08-02 | 5.5b Private playlists — 404 or 403 for another user's GET? | 404 (`PLAYLIST_NOT_FOUND`) — same code as a missing playlist, so a private list's existence is never leaked. Shared (`isPublic`) playlists are readable by any student. Update/delete/add/remove are owner-only and also 404 for non-owners. |
| 2026-08-02 | 5.5b `analytics/me` rating trend before contests ship | Explicit placeholder, not an empty array: `{ available: false, message: '…requires contest history which ships in module 5.5c' }`. No `CodingRating` table yet — contest/rating data lands with 5.5c. |
| 2026-08-02 | 5.5b Progress/analytics caching? | None by design. `progress/me`, `analytics/me`, per-sheet progress and per-problem completion are all computed live from `SolvedProblem`/`Submission` rows on every request via the pure calculators (`progress.calculator.ts`, `analytics.calculator.ts`). Sheet progress intersects the sheet's problems with the user's live `SolvedProblem` ids. |
| 2026-08-02 | 5.5b Curated sheets seed data | New `seedDsaCatalog` seeds a 12-problem real catalog (Two Sum, Valid Parentheses, …) with test cases and upserts 5 curated sheets (Blind 75, Grind 169, NeetCode 150, Google, Amazon) mapped by slug — no empty/fake sheets (e2e asserts a sheet with real problems yields live progress). `prisma/seed.ts` is idempotent. |
| 2026-08-02 | 5.5b Profile visibility `filterVisibleFields` reuse | Implemented as an isolated, unit-tested pure function (`profile-visibility.ts`) over a `DsaCandidateProfile` + `DsaVisibilitySettings`. 5.5b only persists/toggles settings (`GET/PATCH /dsa/profile/visibility`, `showEmail` defaults off); the Recruiter Portal (5.17) will call the same predicate when rendering candidate cards. |
| 2026-08-02 | 5.5c **Rating formula (Elo-style, rank-based)** | `E = (1/(N−1))·Σ_j 1/(1+10^((r_j−R)/400))` (expected fraction of opponents beaten), `S = (N−rank)/(N−1)` (actual fraction beaten; winner=1, last=0), `Δ = round(K·(S−E))` with **K=40** for a participant's first **10** rated contests (provisional, faster convergence) and **K=20** after. New rating clamped to **[100, 4000]**; initial rating **1200**. Tiers: Beginner 100–1199, Amateur 1200–1399, Intermediate 1400–1599, Advanced 1600–1799, Expert 1800–1999, Master 2000–2399, Grandmaster 2400–2799, Legendary 2800+. Reasoning: Elo's logistic expectation is the standard, defensible choice; using finishing rank (not raw points) for `S` makes the ordering what matters; provisional K lets new users converge to their true level quickly. Implemented as the pure `computeRatingUpdate` in `src/dsa/competition/rating-calculator.ts` with fixed input/output vectors. |
| 2026-08-02 | 5.5c **Contest time-boundary rule** | A submission tagged with `contestId` counts toward that contest's leaderboard/rating **only if its grading completes (verdict "lands") within [startTime, endTime]**. A submission that lands after `endTime` — even one that was in flight when the contest ended — is still graded and the user still gets their verdict, but it is excluded from the contest's ranking and rating. Decided in the worker after grading finishes, using the submission's `completedAt`. |
| 2026-08-02 | 5.5c **Leaderboard tie-break rule** | Order = **(total points DESC, last accepted submission time ASC)**. Both dimensions are packed into one Redis zset score as `points·10⁷ + (10⁷ − 1 − elapsedSecondsSinceContestStart)` — the tie-break component occupies `[0, 10⁷−1]`, so an untouched participant (0 points, 0 elapsed) encodes exactly `0·10⁷` and decodes back to 0/0 (the `−1` removes the off-by-one where a bare `10⁷` would decode to 1 point). `ZREVRANGE` ordering therefore exactly equals contest ordering with no secondary sort (elapsed seconds stay well below 10⁷ ≈ 115 days). The composite is produced by the pure `contestCompositeScore` and tested directly, not as an incidental effect of insertion order. |
| 2026-08-02 | 5.5c **`CodingAchievement` deferred to 5.11 (Gamification)** | Not built here. The original list's DSA achievements ("First Accepted", "100 Problems Solved", "Contest Winner") are the same underlying feature as Gamification's badges (5.11) — same overlap already resolved for `ReadinessScore`/`ReadinessPrediction` in 5.4. The raw data 5.11 needs is already cleanly queryable from this session's tables (`Submission`, `SolvedProblem.firstSolvedAt`, `CodingRating`, `CodingRatingHistory`, `ContestParticipant` placements); badge/trigger logic ships with 5.11. |
| 2026-08-02 | 5.5c **Anti-cheat generic schema** | Single `anti_cheat_events` table, not a DSA-contest-specific one: `sourceType` (`dsa-contest` | `coding-battle` | `practical-exam`) + `sourceId` + `eventType` (tab-switch, copy-paste, …) + `detail`. Coding Battles (5.9) and the deferred practical-exam work (5.28b) log the same shape against the same table. |
| 2026-08-02 | 5.5c **`RankingService` reusable design** | Standalone `src/dsa/ranking.service.ts` wrapping Redis sorted sets generically (`upsert`, `remove`, `top`, `rankOf`, `around`, `scoreOf`, `count`, `reset`) with caller-supplied keys — no contest knowledge inside. Contest leaderboards pass the encoded composite score above; 5.10 (global/college/dept/company/weekly/monthly leaderboards) reuses it as-is. |
| 2026-08-02 | 5.5c **Contest CRUD scoping** | Create/update/delete: `admin`, `college_admin`, `faculty` (faculty + college_admin get `create/update/delete:dsa-contests` permissions). Read contests + leaderboard: all four roles. Register: students only (`create:dsa-registrations`). Rating endpoints: students only. Contest submissions reuse the 5.5a pipeline via an optional `contestId` on `Submission` — no parallel judging path. |
| 2026-08-02 | 5.5c **Rating finalization timing** | Rating changes are applied **once** when a rated contest ends, triggered lazily on the first read after `endTime` (leaderboard GET or `rating/me`), guarded by an atomic `standingsFinalizedAt` claim (`updateMany … where finalizedAt is null`) so concurrent readers can never double-apply. A `CodingRatingHistory` row records every change (rank, before/after, contest). |
| 2026-08-02 | 5.5c `analytics/me` rating trend becomes real | The 5.5b placeholder is replaced now that contest history exists: `computeAnalytics` accepts optional `ratingHistory` and returns `{ available: true, points: [{contestName, rank, ratingBefore, ratingAfter, change, date}] }`; with no rated contests it returns `{ available: false, message: 'No rated contest history yet' }`. 5.5b's e2e assertion updated to match. |
| 2026-08-02 | 5.5c **Module complete** | Full unit suite 65 suites / 721 tests green (coverage 92.61% stmts / 81.05% branch / 87.09% funcs / 92.37% lines ≥ 85/80/85/85 gates); full e2e 10 suites / 188 tests green; lint + `tsc --noEmit` + `nest build` clean. 5.5c adds: contest CRUD, registration, contest-tagged submissions with post-end grading (verdict kept, standings excluded), composite-score live leaderboard with tie-break, one-shot Elo finalization, rating history + real `analytics/me` trend, generic anti-cheat events, reusable `RankingService`. Migration `20260802040638_add_dsa_compete` applied. |
| 2026-08-02 | 5.6 **Report generation strategy** | **Fresh AI summarization over the full transcript** (not mechanical aggregation of per-turn feedback). Quality: a real synthesized evaluation reads coherently across the whole interview instead of stitching turn scores together. Truncation strategy for long interviews (pure `buildInterviewTranscript` in `src/interviews/report-builder.ts`): always keep the opening AI turn as context, then keep at most the **most recent 40 turns**; each turn's text is capped at 800 chars; the builder returns a `truncated` flag so the caller can log it. If the report AI call fails at `end`, the session **still ends** (the end transition is the user's intent and must never be blocked by a downstream AI failure) and the report is lazily **regenerated on first `GET /report`** — so a failed finalization is retryable, never stuck. |
| 2026-08-02 | 5.6 **Voice/video contract** | `mode` is a free-form string in the DTO (not an `@IsIn(['text'])`), because restricting it in the DTO would surface a 400 validation error, which would silently imply the mode is supported. Instead the service validates: any `mode !== 'text'` → **501 `MODE_NOT_AVAILABLE`** with message `Voice and video interview modes are not available yet. Please use text mode.` — explicit, not a silent no-op, not a fake transcript. Tested at both unit and e2e level. |
| 2026-08-02 | 5.6 **Report accessibility rule** | A report is only accessible **after** the session is ended. `GET /report` on a `created`/`in_progress` session → **409 `SESSION_NOT_ENDED`**. After `end`, the report is returned (or lazily generated if the end-time generation failed). |
| 2026-08-02 | 5.6 **DSA interviews reuse the real pipeline** | `type: dsa` sessions require a `problemId` (404 `PROBLEM_NOT_FOUND` if unknown) that links to a real Problem from the 5.5a catalog. A turn may carry `code` + `languageId`; when present (dsa-type only), the code is graded **synchronously via the shared `JudgeService`** against the problem's hidden test cases and the verdict/passed/total feed the next AI prompt and are stored on the turn. No parallel coding content or execution path. |
| 2026-08-02 | 5.6 **Technical interviews pull `SkillProfile` context** | For `type: technical`, the AI prompt is seeded with the candidate's `SkillProfile` rows (name, category, proficiency, years of experience, isPrimary) so the interviewer actually targets the candidate's stated skills instead of asking generically — the `type` field changes the prompt, not just the label. |
| 2026-08-02 | 5.6 **Session state machine** | `created` → `in_progress` → `ended`. `POST /sessions` generates the AI opening question (AI call #1) and stores it as the first AI turn; if that call fails nothing is persisted and creation returns 502. First `POST /turns` transitions `created`→`in_progress`. `POST /turns` on `ended` → **409 `SESSION_ENDED`**. Each turn POST is a transaction that persists the user answer + the AI's next question + per-turn `InterviewFeedback` rows atomically — a failed/malformed AI response persists **nothing**, leaving the session cleanly retryable. `POST /end` is idempotent (200 with `reportAvailable`). |
| 2026-08-02 | 5.6 **AiService reuse confirmed** | The 5.4 `AiService` (`AiModule`, global) is reused as-is for opening-question, turn, and report generation. No second AI wrapper. JudgeService (global) reused for DSA grading. Interview data access lives in its own `InterviewRepository` (which reads the linked Problem's hidden test cases directly via Prisma, consistent with the per-module repository pattern). |
| 2026-08-02 | 5.6 **AI failure surfacing** | `AiServiceError` (a plain `Error`) would have leaked as a generic 500 through the global filter. The interview service wraps every AI call in `callAi`, normalizing `AiServiceError` to a **502 `AI_GENERATION_FAILED`** envelope — clean and retryable for the client, and the state machine never half-commits because state is only persisted after the call resolves. |
| 2026-08-02 | 5.6 **Module complete** | Full unit suite 69 suites / 773 tests green (coverage 92.95% stmts / 81.19% branch / 87.67% funcs / 92.73% lines ≥ 85/80/85/85 gates — branch coverage **recovered** from the 5.5c 81.05% + a new `interview.repository` spec took the interviews module to 81.81% branch); full e2e 11 suites / 205 tests green; lint + `tsc --noEmit` + `nest build` clean. 5.6 adds: session CRUD/history, opening-question generation, turn loop with per-turn feedback rows, synchronous DSA grading via JudgeService, end + lazily-retried report generation, text-mode-only contract with explicit 501 for voice/video, state-machine enforcement (409 after end, report only after end), AI-failure resilience, `InterviewSession`/`InterviewTurn`/`InterviewFeedback`/`InterviewReport` models. Migration `20260802044313_add_ai_interview` applied. |
| 2026-08-02 | 5.7 **Career roadmap vs 5.4 placement roadmap — kept distinct** | 5.4's `generateRoadmap` is a **tactical, weekly, placement-prep executor** (10 weeks × 7 daily tasks, DSA/aptitude/company-prep) aimed at immediate placement. 5.7's Career Roadmap is the **long-horizon strategic trajectory** (months-to-years, phase-based: foundations → skill mastery → projects/portfolio → interview prep → offers), anchored on the shared `CareerGoal` (targetRole, targetCtcLakhs, targetDate). Different entities, different cadence, different consumer. Implemented as a pure `generateCareerRoadmap` (deterministic, no AI) returning phase items persisted to `CareerRoadmapItem`. Not a restatement of 5.4. |
| 2026-08-02 | 5.7 **Salary prediction framing + 5.16 overlap note** | The prediction is a **personalized estimate for the current candidate**, computed from their `SkillProfile`, years of experience, active `CareerGoal` (targetRole / targetCompanies / targetCtcLakhs). Every response is explicitly labeled `isEstimate: true` with a `confidence` (0–100), a `rangeLow/HighLakhs`, and a `factors` list explaining what drove it — never presented as hard market data. 5.16 (Company Prep Paths) will later hold general per-company salary *reference* data; **revisit this endpoint to blend that real reference data in** once 5.16 exists rather than relying on an AI guess indefinitely (recorded, tracked). |
| 2026-08-02 | 5.7 **Skill-gap is deterministic, not AI** | `computeSkillGap` is a pure set-difference over the candidate's real `SkillProfile` names vs a curated `TARGET_SKILLS_BY_ROLE` catalog (role → required skills) for the goal's `targetRole`. Pure + isolated, unit-tested with fixed candidate/target pairs. AI is only used where the spec allows it (chat, salary estimate); roadmap and recommendations are also deterministic. `SkillGap` rows persist the latest assessment (upsert per user). |
| 2026-08-02 | 5.7 **Chat streaming decision — request/response now, SSE documented as future** | The OpenCode Zen backend (`/v1/responses`, OpenAI Responses API) **does support SSE streaming** (`stream: true`), confirmed against the docs. However: the 5.4/5.6 `AiService` is a single-shot client; true streaming needs a new SSE-parsing client method, an `@Sse()` controller route, a fake that emits tokens in CI (which can't exercise a live stream anyway), and complicates the atomic message-persistence guarantee. Per the "either is fine, an undocumented silent fallback is not" guidance, this module ships `POST /career-coach/chat` as a **solid request/response endpoint** with full history persistence + 5.6-style AI-failure resilience, and records **true token streaming via `@Sse()` over the confirmed SSE backend as an explicit future item**. No silent non-streaming fallback — it's the documented contract for now. |
| 2026-08-02 | 5.7 **`CareerGoal` reused, not re-owned** | No new goal table. The module reads the shared 5.2 `CareerGoal` via its own repository (consistent with the per-module repository pattern) and anchors roadmap / salary / skill-gap on the student's active goal. CRUD stays in Student Platform (5.2). |
| 2026-08-03 | 5.7 **Module complete** | Full unit suite 75 suites / 826 tests green (coverage 93.26% stmts / 81.47% branch / 88.16% funcs / 93.07% lines ≥ 85/80/85/85 gates); full e2e 12 suites / 217 tests green; lint + `tsc --noEmit` + `nest build` clean. 5.7 adds: pure `computeSkillGap` + `resolveTargetSkills` (role/company/industry catalog), pure `generateCareerRoadmap` (5 long-horizon phases, duration budgeted to the goal's target date, clamped 3–48 months), pure `generateLearningRecommendations`, AI salary prediction (upserted `SalaryPrediction`, `isEstimate: true` + confidence + range + factors), `POST /career-coach/chat` request/response with atomic user+assistant persistence and 502 `AI_GENERATION_FAILED` on failure (nothing persisted), paginated chat history, `CareerRoadmapItem`/`LearningRecommendation`/`SalaryPrediction`/`SkillGap`/`CareerChatMessage` models, 5 new student permissions. Migration `20260802050432_add_career_coach` applied. |

| 2026-08-03 | 5.8 **PDF export — `@react-pdf/renderer`, not Puppeteer** | **Chosen: `@react-pdf/renderer`.** Why not Puppeteer: the Dockerfile is `node:22-alpine`; Puppeteer needs a bundled Chromium + dozens of system libs (`apk add chromium` + 500MB+), bloating the image and forcing a CI browser download on every e2e run. `@react-pdf/renderer` is pure JS — no browser, no native deps, works in the alpine container and in Testcontainers e2e unchanged. Cost: templates are rendered with its component API rather than arbitrary HTML/CSS. Mitigated by driving it via `React.createElement` (no JSX/tsconfig change, since this repo's build is plain `tsc` and jest only transforms `.ts`/`.js`) and by keeping template "style" as declarative JSON (fonts, colors, layout) that the builder maps to components. PDF output is validated in e2e by parsing the generated document and asserting real text is present — not just "endpoint didn't throw". |
| 2026-08-03 | 5.8 **DOCX export — `docx`, not docxtemplater, independent of the PDF choice** | **Chosen: `docx`** (programmatic OOXML document builder). DOCX is a separate technical problem from PDF — no PDF tool emits it as a side effect. Rejected docxtemplater: it needs a maintained binary `.docx` template with `{placeholders}` and a full ZIP/parsing layer; `docx` builds the document tree in TypeScript directly from the same content model the PDF builder uses, is pure JS, and is well-tested. DOCX validity is asserted in e2e by unzipping the produced buffer and parsing `word/document.xml` for expected content. |
| 2026-08-03 | 5.8 **Exports generated on demand; no S3/object-storage archival by default** | `GET /resumes/:id/export?format=pdf|docx` regenerates from the current (or a specific historical version's, via `?versionId=`) content on every request and returns the buffer with the right content-type + `Content-Disposition`. A stored file goes stale the moment content changes, so nothing is persisted to S3. An immutable "exactly what was submitted to Company X on date Y" archive is a real feature but belongs to the 5.13 application/submission flow, not the base export endpoint — deferred, not silently skipped. |
| 2026-08-03 | 5.8 **ATS scoring split: deterministic layer + AI suggestions layer** | A meaningful chunk of ATS-friendliness is purely checkable, so the numeric score is a **pure, unit-tested function** (`computeAtsScore`): section presence (contact/skills/experience/education/…), ATS-hostile elements (tables, images, text-boxes, columns), text density, and keyword overlap against an optional supplied job description. `AiService` is reserved only for the **qualitative prose suggestions** layered on top (same principle as 5.4 roadmap / 5.7 skill-gap). The AI layer degrades gracefully: if it fails, the deterministic score + issues still persist with `suggestionsStatus: 'failed'` (503 `AI_SUGGESTIONS_FAILED`), never silently dropping the score. |
| 2026-08-03 | 5.8 **Versioning semantics — snapshot on every save + restore creates a NEW version** | Every content-changing create/update auto-snapshots a `ResumeVersion` (incremented per resume); explicit checkpoints aren't needed because the save itself is the snapshot. `POST /resumes/:id/versions/:versionId/restore` **creates a new version** from the restored content — it never overwrites or deletes history (standard VCS behavior). A dedicated test proves restoring an old version leaves every prior version intact and adds exactly one new one. |
| 2026-08-03 | 5.8 **`GET /resumes/:id/score` vs `POST /ats-analysis`** | `POST /ats-analysis` runs the deterministic scorer + AI suggestions and persists an `AtsAnalysis` row. `GET /score` is a lightweight re-read of the persisted deterministic score (no AI), so a student can re-fetch it cheaply; `GET /suggestions` re-reads the persisted AI suggestions. A re-run via `POST` refreshes both. |
| 2026-08-03 | 5.8 **Module complete** | Full unit suite 84 suites / 896 tests green (coverage 92.98% stmts / 81.16% branch / 87.63% funcs / 92.8% lines ≥ 85/80/85/85 gates); full e2e 13 suites / 240 tests green; lint + `tsc --noEmit` + `nest build` clean. 5.8 adds: resume CRUD scoped to the owner, three seeded templates (`modern`/`classic`/`technical`) with declarative JSON style, snapshot-on-save `ResumeVersion` history (restore creates a NEW version — history never rewritten), pure `computeAtsScore` (section presence, ATS-hostile elements, text density, optional job-description keyword overlap) + AI suggestions layered via `AiService` with graceful degradation (score persists even when AI fails → 503 `AI_SUGGESTIONS_FAILED`), `GET /score` / `GET /suggestions` re-reads of the persisted analysis, on-demand `GET /export?format=pdf|docx` (via `@react-pdf/renderer` and `docx`) regenerated from current or `?versionId=` historical content. Two app-wide fixes: `LoggingInterceptor` no longer crashes on `@Res()` endpoints that already sent headers (`headersSent` guard), and e2e exports force `.responseType('blob')` so binary DOCX/PDF bodies are asserted as buffers. `Resume`/`ResumeVersion`/`ResumeTemplate`/`AtsAnalysis` models + 9 new student permissions. Migration `20260803062418_add_resume_ats` applied. |

| 2026-08-04 | 5.9 **Matchmaking — deterministic pairing function, not ad-hoc** | Pure `pairPlayers(players, now, params)` in `src/battles/matchmaking/matchmaking.ts`: sorts by rating, greedily pairs each unpaired player with the closest-rated opponent within a symmetric window (`min(windowI, windowJ)`). `allowedWindow` = `baseWindow(50) + elapsed * growthPerSecond(10)` capped at `maxWindow(400)`. Both players must be within each other's window (symmetric check). Returns pairs in order of discovery (ascending rating). Deterministic and unit-tested with fixed queue-state cases: empty → [], single → [], two close → pair, two far → no pair, three → one pair + leftover, window widens over time, symmetric window enforcement, deterministic output. |
| 2026-08-04 | 5.9 **Matchmaking tick — BullMQ repeatable job, every 5 seconds** | Registered in `BattlesService.onModuleInit` via `matchmakingQueue.add('matchmake', {}, { repeat: { every: 5_000 } })`. Tick calls `runMatchmakingTick()`: snapshots queue (Redis zset scored by rating + hash for joinedAt), runs `pairPlayers`, then atomically claims pairs via `multi().zrem().hdel().exec()` — if both zrem return 1, the claim succeeds and the battle is created; if either returns 0 (concurrent tick took them), skip. Also called synchronously from `joinQueue` for fast first-match. Redis queue (`RankedQueue`) uses sorted set + hash for atomic add/remove/claim. |
| 2026-08-04 | 5.9 **Battle types and lifecycle** | Three types: `ranked` (matchmade, isRated=true, moves rating), `practice` (solo, not rated), `private` (creator generates invite code, invitee joins via code, not rated). States: `pending` (private awaiting join) → `active` (startedAt + endsAt set) → `finished` (winner decided, rating applied for ranked) / `cancelled`. Practice and private go straight to active. Private transitions from pending to active when second player joins. |
| 2026-08-04 | 5.9 **Private battle invite mechanism** | `POST /battles/private` creates battle with `status: 'pending'`, generates a 6-character alphanumeric invite code (unique, collision-checked up to 5 attempts). Returns `{ battleId, inviteCode }`. `POST /battles/private/join { inviteCode }` validates code, checks battle is pending + not full + user not already in, adds participant, transitions to active, emits `battle.start` to both users. |
| 2026-08-04 | 5.9 **Battle submissions reuse 5.5a pipeline** | `POST /battles/:id/submit` → `BattlesService.submitCode` → `DsaService.submitCode(userId, problemId, { sourceCode, languageId, battleId })`. DsaService detects `battleId`, calls `battles.assertSubmittable`, creates Submission with `battleId`, queues to existing `dsa-submissions` BullMQ queue. DsaSubmissionsProcessor grades via JudgeService, calls `battles.onSubmissionGraded` (same hook pattern as 5.5c contests). |
| 2026-08-04 | 5.9 **Battle end-time boundary — mirrors 5.5c contest rule** | A submission graded after `endsAt` is still graded + verdict delivered, but excluded from battle result + rating (checked via `completedAt > endsAt` in `onSubmissionGraded`). Battles also support early end: first solved → battle ends immediately (winner determined). Lazy finalization on reads (`getBattle`, `getMyRating`, `getHistory`) triggers `finalizeDueBattles` for any ranked battle past its endsAt. |
| 2026-08-04 | 5.9 **Rating update for ranked battles — shared `computeRatingUpdate`** | 1v1 Elo: winner rank=1, loser rank=2, draw rank=1.5. `computeRatingUpdate({ currentRating, contestsParticipated, userRank, opponentRatings: [opponentRating] })`. Same K=40 provisional / K=20 standard, same [100, 4000] clamp. RatingHistory rows tagged `source: 'battle'` with `battleId` — distinct from contest rows. `CodingRating` updated via upsert. Global rating zset (`dsa:rating:global`) updated. |
| 2026-08-04 | 5.9 **Practice/private do not move rating — explicit test** | Battle finalization skips rating application for `type !== 'ranked'`. E2e tests create practice + private battles, force end via DB update, call `GET /battles/ratings/me` → rating stays at 1200, no rating history rows created. |
| 2026-08-04 | 5.9 **WebSocket events — user-room delivery via DsaGateway** | Extended `DsaGateway` with `emitToUser(userId, event, payload)` (reuses `user:{id}` room). Battle events: `battle.start` (battleId, type, problem, durationSeconds, endsAt, opponents), `battle.progress` (battleId, userId, submissionId, verdict, solved, elapsedSeconds), `battle.end` (battleId, winnerId, status, participants). No second gateway. Two-client WS e2e: both clients connect, both receive `battle.start` with same battleId + problem, submit on one → both receive `battle.end` with winner. |
| 2026-08-04 | 5.9 **Module complete** | Full unit suite 86 suites / 929 tests green; full e2e 14 suites / 250 tests green; lint + `tsc --noEmit` + `nest build` clean. 5.9 adds: `Battle`/`BattleParticipant` models, `battleId` on `Submission`, `source`+`battleId` on `CodingRatingHistory`, pure `pairPlayers` matchmaking function + unit tests, Redis `RankedQueue` with atomic claim, BullMQ repeatable tick (5s), ranked/practice/private battle creation, invite-code private join, battle submission via DsaService pipeline, battle end-time boundary (late graded, excluded), lazy finalization, 1v1 Elo rating via shared `computeRatingUpdate`, 7 new student permissions (`create/delete:battle-queue`, `create/read:battles`, `create:battle-submissions`, `read:battle-ratings`, `read:battle-history`), `emitToUser` on DsaGateway, two-client WS e2e. Migration `20260803092502_add_coding_battles` applied. |
